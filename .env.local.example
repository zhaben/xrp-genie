# Network Configuration (will be updated by CLI based on user selection)
# Options: testnet, mainnet
NEXT_PUBLIC_XRPL_NETWORK=testnet
# Options: devnet, mainnet
NEXT_PUBLIC_WEB3AUTH_ENV=devnet

# Web3Auth Client ID is configured directly in the code
# It's safe to expose publicly - no environment variable needed